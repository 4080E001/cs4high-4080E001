#include <stdio.h>
int main(){
    int number,N;
    printf("N= ");
    scanf("%d",&N);
    int isprime;
    for(number=2; number<N; number++){
    if(N % unmber == 0){

    }
    }
    return 0;
}
