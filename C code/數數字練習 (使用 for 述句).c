#include <stdio.h>
int main(){
    int count,N;
    printf("enter  the number: ");
    scanf("%d",&N);
    for (count=1;count<=N;count++){
        printf("%d\n",count);
    }
    return 0;
}
